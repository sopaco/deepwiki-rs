# 阶段三：文档编排生成详细指南

## 核心原则

1. **基于研究，不臆测**：所有描述必须有阶段二研究报告中的代码证据支撑
2. **Mermaid 图表必须可渲染**：遵循语法规范（见 `doc-templates.md`）
3. **结构完整**：每份文档必须包含规定的章节，不能省略核心章节
4. **语言一致**：使用用户指定的输出语言（默认中文）
5. **代码引用密度**：每个模块文档必须引用至少 3 个具体文件路径，关键组件表格每行必须有文件路径列
6. **分章节增量写入**：大型文档分多次写入，避免单次响应过长
7. **中间产物持久化**：研究报告写入 `.litho-agent/` 临时目录，编排阶段按需读取
8. **跨模块协作叙述**：每个模块文档包含模块在核心业务流程中的角色描述

---

## ⚠️ 中间产物持久化策略

### 问题
Agent 单次对话上下文窗口有限。随着分析深入，早期的研究结果可能因上下文压力被遗忘。

### 解决方案
每完成一个研究步骤，将关键发现写入 `.litho-agent/` 临时目录：

```
.litho-agent/
├── preprocessing.md        ← 阶段一产出
├── c1-system-context.md    ← C1 系统上下文
├── c2-domain-modules.md    ← C2 领域模块
├── architecture.md         ← 架构研究
├── workflow.md             ← 工作流研究
├── boundary.md             ← 边界接口
├── database.md             ← 数据库（条件）
└── modules/                ← 模块深度（逐个写入）
    ├── llm.md
    ├── cache.md
    └── ...
```

**编排阶段读取方法**：
- 需要某报告时 → `read_file` 从 `.litho-agent/` 读取
- 不需要时 → 不读取，节省上下文
- 最终输出完成后 → 删除 `.litho-agent/`（可选保留供复查）

**核心优势**：与 deepwiki-rs 的 Memory 作用域机制等效，但使用文件系统而非内存

---

## ⚠️ 分章节增量写入策略（P0 关键优化）

deepwiki-rs 通过多次独立 LLM 调用生成每个文档。Agent 单次响应容量有限，必须分批写入：

### 大型文档分章节策略

| 文档 | 写入策略 | 分批方式 |
|------|---------|---------|
| `1.概述.md` | 单次写入 | 中等文档，通常可一次完成 |
| `2.架构.md` | **分章节写入** | 第1次：框架+设计理念+架构模式表+技术栈 → 第2次：C4图+模块职责表 → 第3次：关键流程时序图+技术实现+ADR |
| `3.工作流.md` | **分章节写入** | 第1次：框架+核心路径图 → 第2次：主要工作流流程图+时序图 → 第3次：并发模型+错误处理 |
| `4.Deep-Exploration/*.md` | **逐模块写入** | 每个模块一个独立 write_to_file 调用 |
| `5.边界接口.md` | 单次写入 | 通常可一次完成 |
| `6.数据库概览.md` | 单次写入 | 条件触发时一次性完成 |

### 增量写入操作方法
- 第1次：`write_to_file` 写入完整框架（所有章节标题 + Mermaid 图 + 核心表格）
- 第2次+：`replace_in_file` 在各章节的 `{待补充}` 占位处插入详细内容

---

## Mermaid 图表语法规范（重要！）

### 节点 ID 规则
```
✅ 正确：A, B1, NodeA, myNode
❌ 错误：My Node, node-1, @user, node.1
```

### 节点标签规则（含特殊字符时必须加双引号）
```
✅ 正确：A["用户 (User)"] --> B["系统 @backend"]
❌ 错误：A[用户 (User)] --> B[系统 @backend]
```

### 标签内换行
```
✅ 正确：A["第一行<br/>第二行"]
❌ 错误：A["第一行
第二行"]
```

### Mermaid 图类型选择指南
| 图类型 | 适用场景 | 语法关键词 |
|-------|---------|---------|
| `C4Context` | 系统上下文图（用户+外部系统） | `Person`, `System`, `Rel` |
| `C4Container` | 容器架构图（主要子系统） | `Container`, `Container_Boundary` |
| `C4Component` | 组件图（模块内部） | `Component`, `Rel` |
| `flowchart TD` | 流程图（自上而下） | `-->`, `--\|label\|-->` |
| `flowchart LR` | 流程图（从左到右） | `-->` |
| `sequenceDiagram` | 时序图（跨系统交互） | `participant`, `->>`, `-->>` |
| `graph LR` | 依赖关系图 | `-->` |
| `erDiagram` | 实体关系图（数据库） | `}|--\|{`, `TABLE {` |

---

## Step 3.1：生成 1.概述.md

### 文档结构

```markdown
# 项目概述：{项目名称}

**文档版本：** 1.0
**生成日期：** {YYYY-MM-DD}
**置信度评分：** {X}%

---

## 1. 执行摘要

{2-3 段话描述：项目是什么、解决什么问题、核心能力是什么}

---

## 2. 系统概述

### 2.1 核心目标
{列表形式的核心目标，3-5 条}

### 2.2 业务价值
| 价值维度 | 描述 |
|---------|------|
| {维度1} | {描述} |

### 2.3 技术特性
{列表形式的主要技术特点}

---

## 3. 目标用户和利益相关者

### 3.1 {用户角色1}
**简介**：{描述}
**使用场景**：{列表}
**关键需求**：{列表}

{对每个用户角色重复上述结构}

---

## 4. 系统上下文图（C4 Level 1）

\`\`\`mermaid
C4Context
    title {项目名称}系统上下文图

    Person({userId}, "{用户名}", "{描述}")
    {对每个用户角色重复}

    System({sysId}, "{系统名}", "{描述}")

    System_Ext({extId}, "{外部系统名}", "{描述}")
    {对每个外部系统重复}

    Rel({userId}, {sysId}, "{关系描述}")
    Rel({sysId}, {extId}, "{关系描述}", "{协议}")
    {对每个关系重复}
\`\`\`

### 4.1 关键交互流程
{用文字描述主要使用流程}

---

## 5. 系统范围与边界

### 5.1 范围内
{列表形式的系统能力清单}

### 5.2 范围外（明确排除）
{列表形式的不做的事情}

---

## 6. 核心功能与能力

### 核心能力
{列表形式，使用粗体标注功能名称}

### 高级特性
{列表形式的进阶功能}

---

## 7. 技术栈概览
| 层次/领域 | 技术 | 说明 |
|---------|-----|------|
| {层次1} | {技术} | {说明} |
```

---

## Step 3.2：生成 2.架构.md

### 文档结构

```markdown
# 系统架构文档：{项目名称}

**版本：** 1.0
**分类：** 内部架构文档
**生成日期：** {YYYY-MM-DD}

---

## 1. 架构概述

### 1.1 设计理念

{基于研究报告中识别的架构模式，描述 2-4 个核心设计原则}

**1. {原则名称}**
{描述这个原则在代码中如何体现，可引用具体模块或文件}

**2. {原则名称}**
{描述}

### 1.2 核心架构模式
| 模式 | 实现方式 | 目的 |
|------|---------|------|
| {模式1} | {如何实现} | {为什么} |

### 1.3 技术栈概述
{详细的技术栈描述}

---

## 2. 系统上下文（C4 Level 1）

\`\`\`mermaid
C4Context
    {复用 1.概述.md 中的 C4Context 图，或精简版}
\`\`\`

---

## 3. 容器视图（C4 Level 2）

\`\`\`mermaid
C4Container
    title 容器架构 - {项目名称}

    Person({userId}, "用户")

    System_Boundary({sysName}, "{系统名} 应用") {
        Container({containerId}, "{容器名}", "{技术}", "{描述}")
        {对每个主要容器/模块重复}
    }

    System_Ext({extId}, "{外部系统}", "{描述}")

    Rel({src}, {dst}, "{关系}")
    {对每个关系重复}
\`\`\`

### 3.1 领域模块职责
| 模块/领域 | 路径 | 职责 | 关键抽象 |
|---------|------|------|---------|
| {模块1} | `src/xxx/` | {描述} | {接口/类型名} |

---

## 4. 组件视图（C4 Level 3）

### 4.1 {核心模块1}组件架构

\`\`\`mermaid
graph LR
    {用 graph 描述模块内组件关系}
\`\`\`

**组件职责：**
{列表形式描述每个组件的职责}

### 4.2 {核心模块2}组件架构
{重复}

---

## 5. 关键流程

### 5.1 {主要工作流}

\`\`\`mermaid
sequenceDiagram
    participant {P1} as {名称1}
    participant {P2} as {名称2}
    {P1}->>{P2}: {操作}
    {P2}-->>{P1}: {返回}
\`\`\`

### 5.2 {次要工作流}
{重复}

---

## 6. 技术实现

### 6.1 关键架构模式
{描述代码中实现的主要设计模式，可包含代码片段（伪代码或实际代码）}

### 6.2 并发和并行策略
{描述系统如何处理并发}

### 6.3 性能优化策略
{描述缓存、批处理、懒加载等性能优化手段}

---

## 附录：架构决策记录（ADR）

**ADR 1：{标题}**
- **决策**：{做了什么决策}
- **原因**：{为什么这样决策}
- **后果**：{带来的影响}

{重复}
```

---

## Step 3.3：生成 3.工作流.md

### 文档结构

```markdown
# 核心工作流

## 1. 工作流概述

### 1.1 系统架构与工作流理念
{简要描述系统的工作流理念}

### 1.2 核心执行路径

\`\`\`mermaid
flowchart LR
    subgraph 输入层
        A[{输入1}] --> B[{处理}]
    end
    subgraph 处理层
        C[{步骤1}] --> D[{步骤2}]
    end
    subgraph 输出层
        E[{输出}]
    end
    输入层 --> 处理层 --> 输出层
\`\`\`

---

## 2. 主要工作流

### 2.1 {主工作流名称}

#### 流程图

\`\`\`mermaid
flowchart TD
    Start([开始]) --> Step1[{步骤1}]
    Step1 --> Decision{判断条件?}
    Decision -->|是| Step2[{步骤2}]
    Decision -->|否| Step3[{步骤3}]
    Step2 --> End([结束])
    Step3 --> End
\`\`\`

#### 时序图

\`\`\`mermaid
sequenceDiagram
    participant {P1} as {组件1}
    participant {P2} as {组件2}
    participant {P3} as {组件3}

    {P1}->>{P2}: {操作1}
    {P2}->>{P3}: {操作2}
    {P3}-->>{P2}: {返回2}
    {P2}-->>{P1}: {返回1}
\`\`\`

#### 阶段说明
| 阶段 | 执行者 | 输入 | 输出 | 说明 |
|------|-------|------|------|------|
| {阶段1} | {模块} | {输入} | {输出} | {说明} |

{对每个主要工作流重复}

---

## 3. 并发与异步模型
{描述系统的并发处理方式}

## 4. 错误处理策略
{描述错误处理和优雅降级}
```

---

## Step 3.4：生成 4.Deep-Exploration/{模块名}.md

### ⚠️ 全覆盖要求

Agent 必须为领域模块报告中识别到的**每一个模块**生成独立的深度文档。这与 deepwiki-rs 的 KeyModulesInsightEditor 一致——它对每个 domain_module 并行调用一次 LLM 生成文档。

**执行策略**：
- 逐模块分析、逐模块写入（每完成一个模块立即 `write_to_file`）
- 每批 3-4 个模块，避免上下文溢出
- 完成一批后继续下一批，直到所有模块覆盖完毕

### 代码引用密度要求（P1）

每个模块深度文档必须满足：
- **至少 3 个具体文件路径引用**（如 `src/llm/client/mod.rs`）
- **关键组件表格每行必须有文件路径列**
- **至少引用 2 个具体的类型名/函数名/Trait 名**（如 `LLMClient`、`AgentBuilder`、`extract()`）
- Mermaid 图节点标签应包含具体名称（如 `"LLMClient<br/>统一接口"`），而非仅 `"LLM模块"`

### 文档结构（每个核心模块一份）

```markdown
# {模块名称}领域

**模块路径**：`src/{module_path}/`
**生成日期**：{YYYY-MM-DD}
**分析置信度**：{X}/10

---

## 概述

{职责描述，2-3 段，要具体，基于代码分析而非臆测}

---

## 核心功能点

1. **{功能1}**：{描述，引用具体函数/类型}
2. **{功能2}**：{描述，引用具体函数/类型}
...

---

## 关键组件

| 组件/类型 | 文件路径 | 核心职责 |
|---------|---------|---------|
| `{Component1}` | `src/.../file.rs` | {描述} |
| `{Component2}` | `src/.../another_file.rs` | {描述} |

---

## 内部数据流

\`\`\`mermaid
flowchart TD
    A["{输入}"] --> B["{处理步骤1}<br/>{具体函数名}"]
    B --> C["{处理步骤2}<br/>{具体函数名}"]
    C --> D["{输出}"]
\`\`\`

**关键步骤说明**：
1. {步骤1}：由 `{函数名}` 处理，位于 `src/.../file.rs`
2. {步骤2}：由 `{函数名}` 处理，位于 `src/.../another_file.rs`

---

## 关键接口与扩展点

{如果模块有可插拔设计，描述接口定义和扩展机制，引用具体 Trait/接口名}

---

## 与其他模块的交互

| 交互模块 | 方向 | 接口/协议 | 说明 |
|---------|------|---------|------|
| {模块A} | 依赖 | `{具体接口名}` | {说明} |
| {模块B} | 被依赖 | `{具体接口名}` | {说明} |

---

## 跨模块协作场景

> 本模块在核心业务流程中的角色（引用领域模块报告中的 business_flows）

**在 {业务流程1} 中**：本模块负责 {角色描述}。具体参与：
- {步骤X}：调用 `{函数名}` 与 {模块Y} 协作完成 {操作}
- {步骤Z}：接收 {模块W} 的输入 `{类型名}`，执行 {处理}

**在 {业务流程2} 中**：本模块作为 {角色} 提供 {能力描述}。

---

## 性能考量

{并发策略、缓存策略、资源管理等}

---

## 实现亮点

{值得注意的设计决策、算法或代码实现技巧}

---

**分析置信度说明**：{X}/10 — {说明置信度原因，如"基于3个核心文件的深度分析，但辅助模块仅概览级覆盖"}
```

---

## Step 3.5：生成 5.边界接口.md

### 文档结构

```markdown
# 系统边界接口文档

本文档描述系统的外部调用接口，包括 CLI 命令、API 端点、配置参数和其他边界机制。

---

{对每种接口类型重复以下结构}

## {接口类型，如 "命令行接口 (CLI)"}

### {命令/接口名称}

**描述**：{功能描述}

**参数**：
| 参数 | 类型 | 是否必须 | 默认值 | 说明 |
|------|------|---------|-------|------|
| {param} | {type} | 是/否 | {default} | {描述} |

**使用示例**：
\`\`\`bash
{示例命令}
\`\`\`

---

## 配置结构

{如果系统有配置文件，描述配置结构}

**示例配置**：
\`\`\`toml/yaml/json
{示例配置}
\`\`\`

---

## 集成建议

{如何将该系统集成到其他系统或 CI/CD 流水线中的建议}
```

---

## Step 3.6：生成 6.数据库概览.md（条件执行）

### 文档结构

```markdown
# 数据库概览

---

## 统计摘要
| 类型 | 数量 |
|------|------|
| 数据表 | X |
| 视图 | X |
| 存储过程 | X |

---

## 实体关系图

\`\`\`mermaid
erDiagram
    {TABLE_A} {
        int id PK
        string name
        int b_id FK
    }
    {TABLE_B} {
        int id PK
        string title
    }
    {TABLE_A} }|--|| {TABLE_B} : "belongs_to"
\`\`\`

---

## 核心数据表

### {表名}
| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| {col} | {type} | PK/FK/NN | {描述} |

---

## 视图、存储过程、函数

{如适用，逐一描述}
```
